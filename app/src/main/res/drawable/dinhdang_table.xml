<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!-- độ rộng viền và màu -->
    <stroke
        android:width="2dp"
        android:color="#FF000B"></stroke>
    <!-- chỗ nào set padding -->
    <padding
        android:bottom="1dp"
        android:left="10dp"
        android:right="1dp"
        android:top="1dp"></padding>
    <!-- chỗ này set độ cong góc -->
    <corners android:radius="40dp"></corners>

</shape>